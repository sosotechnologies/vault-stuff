production: true
auth:
  createAdminUser: true
  adminUser: admin
  adminPassword: admin12345
tls:
  enabled: true
  autoGenerated: true
ingress:
  enabled: true
  hostname: keycloak.re-lux.club
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTPS
  servicePort: https
  tls: true
  extraTls:
    - hosts:
        - keycloak.re-lux.club
      secretName: keycloak-tls
postgresql:
  enabled: true
  postgresqlPassword: psqldbpass12345
# spi:
#   truststorePassword: secret
